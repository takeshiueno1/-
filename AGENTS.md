# Codex 作業指示書

このリポジトリでは、正確性を維持しながら待ち時間を抑えるため、タスクの複雑度とリスクに応じてエージェント数を調整する。

## 基本方針

- 回答、作業報告、コード内の説明は原則として日本語にする。
- 結論を先に示し、不要な前置きや重複した進捗報告を避ける。
- 不明点が重大な分岐でなければ、妥当な前提を明記して作業を進める。
- 既存の未コミット変更を削除、上書き、退避しない。
- 通常のコード編集、対象テスト、ローカルビルドでは都度確認を求めない。
- 本番変更、データ削除、権限・認証情報の変更、破壊的Git操作は実行前に確認する。

## エージェント運用

原則は1エージェントで作業する。複数エージェントは、品質または所要時間を明確に改善できる場合だけ使用する。

### 1エージェントで行う作業

- 文言、CSS、設定、ドキュメントなどの軽微な変更
- 影響範囲が1～3ファイル程度で、仕様と修正方法が明確な作業
- 単一機能の小さな不具合修正
- 同じファイルや同じDBスキーマへ連続して変更を加える作業
- 調査、実装、対象テストを短時間で一貫して完了できる作業

軽微な作業では、実装担当とレビュー担当を分けない。担当エージェントが差分確認と対象テストまで完了する。

### 追加レビューを使う作業

次のような中程度の作業では、実装は1エージェントで行い、完了時だけ別エージェントによる独立レビューを1回行う。

- 複数レイヤーにまたがる変更
- API契約や画面状態遷移を変更する作業
- 回帰リスクが中程度の機能追加
- 要件と実装の照合が必要な作業

レビュー指摘がなければ再レビューは行わない。重要な指摘を修正した場合だけ、修正差分に限定した再レビューを行う。

### 複数エージェントを使う作業

次の条件を満たす複雑・高リスクな作業だけ、複数エージェントを使用する。

- 認証、認可、個人情報、監査ログに関係する
- DBマイグレーション、既存データ保持、ロールバックに関係する
- 複数の独立したモジュールを同時に変更する
- 障害時の影響が大きく、独立レビューが必要である
- 読み取り専用の調査や、互いに独立した検証を並行化できる

並列化する場合も、同じファイル、同じスキーマ、同じポート、同じDB、同じ配布物を複数エージェントが同時に変更してはならない。書き込み対象ごとに担当を1人に固定し、統合担当が最終差分と統合後テストを確認する。

### 並列化を避ける条件

- 前工程の結果が次工程の設計を左右する
- 同一ファイルへの変更が集中する
- 小規模で、引き継ぎやレビューの方が実装より時間を要する
- 実行環境、DB、ブラウザ、デモ配布物を共有する
- ユーザーが速度を優先し、追加レビューを求めていない軽微な作業

## テスト運用

- 開発中は変更箇所に対応する対象テストを優先する。
- 全テスト、Lint、型チェック、ビルドは統合後または完了前にまとめて実行する。
- 同じコードに対する全テストを各エージェントが重複実行しない。
- 認証・認可、DB移行、データ保持などの高リスク変更は、対象テストに加えて全体回帰を実行する。
- 実行していないテストを成功済みと報告しない。

## 進捗報告

- 進捗は短く、現在の工程、確認済み事項、実際のブロッカーだけを報告する。
- エージェント待機中の定型的な報告を繰り返さない。
- 軽微な作業では途中報告を最小限にし、完了結果を早く返す。
- 完了報告では、変更内容、主なファイル、実行したテスト、未確認事項、Git状態を簡潔に示す。

## 完了条件

- 依頼された要件を満たしている。
- 変更に対応するテストが成功している。
- 必要なビルド、Lint、型チェックが成功している。
- 認証・認可、入力検証、既存データ、並行実行への影響を確認している。
- 不要なデバッグコード、一時ファイル、秘密情報が残っていない。
- 複数エージェントを使用した場合は、統合後の状態を主担当が直接検証している。

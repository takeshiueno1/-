# 勤怠管理システム

Excel勤務表の入力、計算、案件別集計、帳票表示を忠実に再現する社内向けWebシステムです。初版は正社員・8時間勤務の提供済みExcelを基準にしています。

## 構成

- Frontend: React 19 + TypeScript + Vite
- Backend: Java 21 + Spring Boot 4.1
- Database: PostgreSQL 17
- Local runtime: Docker Compose

すべて無償利用可能なソフトウェアで構成しています。本番では社内サーバー上に配置し、外出先からはVPN経由でアクセスする前提です。

## 必要環境

- Docker EngineまたはDocker Desktop互換環境
- Docker Compose

個別開発する場合はJava 21とNode.js 20.19以降も使用します。

## ローカル起動

PowerShellで推測されにくい値を環境変数へ設定して起動します。

```powershell
$env:DB_PASSWORD='<32文字以上のランダムな値>'
$env:APP_INITIAL_ADMIN_USERNAME='admin'
$env:APP_INITIAL_ADMIN_PASSWORD='<8～15文字の半角英数字>'
docker compose up -d --build
```

起動後、PCでは <http://localhost:4173> を開きます。初回起動時だけ管理者アカウントが作成されます。パスワードはGit管理対象へ保存しないでください。

### 会社PC 1台でデモする場合

会社PCへDocker Desktopまたは会社指定のDocker互換環境を準備し、GitHubからこのリポジトリを取得してください。Gitを利用できない場合は、対象ブランチをZIPでダウンロードして展開できます。取得後は、プロジェクト直下の `会社PCで起動.cmd` をダブルクリックします。

- 初回起動時に会社PC専用のランダムなDB・管理者パスワードを `.env` へ作成します。
- `.env` はGitの管理対象外です。メール、チャット、GitHubへ送信しないでください。
- 一般ユーザー `test / test`、役職者ユーザー `test02 / test02`、50人分のデモデータを作成します。
- 起動完了後、自動的に <http://localhost:4173> を開きます。
- 終了するときは `会社PCで停止.cmd` をダブルクリックします。DBデータは保持されます。
- 実在する社員の個人情報はデモへ持ち込まないでください。

会社PCでソフトウェアのインストールが制限されている場合は、事前に社内のPC管理者へDocker実行環境の導入を依頼してください。

同じWi-Fiに接続したスマートフォンから確認する場合は、Windows PCのIPv4アドレスを使って `http://<PCのIPv4アドレス>:4173` を開きます。例えばPCのアドレスが `192.168.11.14` なら、スマートフォンでは <http://192.168.11.14:4173> を開きます。

- PCとスマートフォンを同じWi-Fiへ接続してください。
- Windows Defender ファイアウォールの確認が表示された場合は「プライベート ネットワーク」だけを許可してください。
- 公衆Wi-Fiやインターネットへ直接公開しないでください。
- Wi-Fiルーターの「端末間通信禁止」や「プライバシーセパレーター」が有効な環境では接続できません。
- PCのIPv4アドレスは `ipconfig` または `Get-NetIPAddress -AddressFamily IPv4` で確認できます。

管理者は「管理者メニュー」から作業を選択します。「利用者管理」では、最初に「新しい利用者」または「登録済み利用者」を選んでから作業します。社員検索では氏名・社員コード・所属・ユーザー名の入力中に候補を表示します。「本人変更待ち」は、仮パスワードから本人用パスワードへの変更がまだ完了していない状態です。

新規利用者と管理者による再設定では、安全な仮パスワードを画面で自動生成できます。仮パスワードでログインした利用者は、勤怠情報へ進む前に本人用パスワードへの変更が必須です。本人が変更したパスワードの有効期限は変更日から1か月です。期限の10日前からログイン時に期限日と残日数を表示し、期限到来後は変更するまで勤怠情報へ進めません。

新規設定・変更・再発行するパスワードは、8～15文字の半角英数字を受け付け、ユーザー名を含めることもできます。推測されやすい定番パスワードは拒否し、ログイン試行制限とBCryptハッシュ保存は維持します。既存利用者の移行を妨げないよう、変更前の旧ルールのパスワードもログイン認証では受け付け、次回変更時に新ルールを適用します。

権限は「一般」「役職者」「管理者」の3区分です。一般は本人の勤務表だけを扱います。役職者は勤務表画面の「社員選択」から他社員の勤怠を参照・更新できますが、削除と管理機能は実行できません。管理者はこれらに加え、削除、利用者管理、Excel取込、会社カレンダー取込を実行できます。代理操作は監査ログへ記録されます。

一般利用者の勤務表に表示する所属・氏名・役職・社員コードは、ログイン中のユーザーに紐づく社員マスターから自動的に取得します。一般利用者が別社員の情報を指定することはできません。社員マスターが未設定の場合は値を推測せず必須エラーとするため、利用者登録時に正しい所属・役職を登録してください。

WG参加可否で「参加」を選択している勤務表にWGを示す業務内容がない場合は、業務欄へWGの開催日時を記載するよう画面で案内します。

ログイン後は「新規で勤務表を作成」「過去分を参照・編集」から作業を選択します。過去のExcel勤務表の取込はホームへ表示せず、管理者メニューの「Excel勤務表取込」だけから実行できます。APIでも管理者権限を検証し、管理者は他社員を取込先に指定できます。

ログイン画面ではユーザーIDとパスワードの未入力・形式を検証し、日本語エラーを表示します。ログイン情報を忘れた場合は社員コードと氏名から管理者へ再発行依頼を送れます。社員コードが分からない場合は、画面の案内に従ってSlackで総務へ問い合わせます。アカウントの存在有無やユーザーIDは未認証画面へ表示しません。

ログイン後に30分間、クリック・キー入力・タッチ・スクロールのいずれも行われなかった場合は、自動的にログアウトして再ログインを求めます。サーバーセッションにも同じ30分の無操作期限を設定しています。
操作時にサーバー側の認証が既に切れていた場合も、保護APIの401応答を画面全体で検知してログイン画面へ戻し、「認証の有効期限が切れました。再度ログインしてください。」と表示します。

勤務表画面の「勤務表を削除」では、確認欄へ `削除する` と入力し、現在ログイン中のユーザー本人のパスワードで再認証します。管理者が他社員の勤務表を削除する場合も、対象社員ではなく操作中の管理者本人のパスワードが必要です。削除は復旧可能な論理削除で、監査・復旧用データはDBに保持します。

画面はスマートフォン専用レイアウトにも対応しています。幅520px以下ではメニューを画面下部のタップナビゲーションへ切り替え、入力欄とボタンを44px以上に拡大します。勤務表の日次入力は表をカード表示へ変換するため、横スクロールに依存せず入力できます。集計・管理画面も1列表示へ切り替わります。

一般・役職者は自分の勤務表を作成・参照できます。管理者自身の勤務表導線は表示せず、管理者メニューの「勤務表の新規作成」「勤務表の参照・編集」から一般・役職者の勤務表を扱います。印刷ボタンは勤務表が表示されているときだけ表示し、印刷CSSも勤務表本体以外を出力対象外にします。各作業画面には前のページへ戻る操作を表示し、長い画面を下までスクロールすると「最上部へ」ボタンを表示します。

## Outlook・Slack連携

勤務表表示中は左メニューの「連絡」から、本人の保存済み勤務表をOutlookへ添付する操作と、Slackで社員へ連絡する操作を行えます。Outlook欄には対象となるExcelファイル名と確認ボタンを表示します。管理者が代理表示している他社員の勤務表では誤送信防止のため連携欄を表示しません。

- Windows PCで最初に `tools\outlook-helper\Outlook連携を開始.cmd` を実行してください。現在のWindowsユーザーだけに `query-attendance-outlook` 専用リンクを登録します。管理者権限は不要です。
- 登録後に「Outlookで送る」を押すと、有効期限2分・一度限りのダウンロードURLをWindowsへ渡し、勤務表Excelを添付した未送信メールをPC版Outlookで直接開きます。宛先を確認して送信してください。
- 管理者が利用者作成または仮パスワード再発行を行った直後は、「Outlookの安全な下書きを開く」から仮パスワードだけを記載した未送信メールを開けます。ユーザーIDは別経路で伝え、本人確認済みの宛先を指定してOutlookの「暗号化」を有効にしてから送信してください。
- 仮パスワード用URLも有効期限2分・一度限りです。平文パスワードはDBや監査ログへ保存せず、現在のパスワードハッシュと一致する場合だけ下書きを作成します。無効化中の利用者には送信できません。
- 未送信メールには、件名「【勤務表提出】対象年月分 氏名」と、挨拶・勤務表の送付案内・確認依頼を含む社内提出用の本文を自動設定します。
- 初回はブラウザーに「このアプリケーションを開きますか」と表示される場合があります。`Outlook連携を開始.cmd` で登録した専用リンクであることを確認して許可してください。
- Web共有APIを使わないため、PC環境で発生していた `permission denied` を回避します。専用URLは推測困難な乱数で生成し、一度使用するか2分経過すると無効になります。
- Windows連携を解除する場合は `tools\outlook-helper\Outlook連携を停止.cmd` を実行します。
- PC版OutlookまたはWindowsの既定メールアプリが `.eml` に関連付けられている必要があります。新しいOutlookでは最新バージョンを使用してください。
- Slack Webhookが設定済みの場合は「Slackで連絡」から連携チャンネルへ投稿します。未設定の場合はSlackのWeb画面を開き、画面上にも「未設定」と表示します。
- 勤務表ファイルそのものはサーバーへ保存せず、エクスポート操作だけを監査ログへ記録します。
- 未保存の入力は含まれないため、「変更をまとめて保存」の完了後に共有してください。
- LAN確認用のHTTP接続ではWeb共有APIを利用できないため、ダウンロードとアプリ起動のフォールバックになります。本番はVPN内でもHTTPSを使用してください。
- Microsoft 365とSlackの既存契約・ワークスペースは別途必要です。このシステム側で追加の有料ライブラリは使用しません。

以下はAPI経由の自動通知機能を併用する場合の任意設定です。秘密情報は `.env` やGitへ登録せず、サーバーのシークレット管理機能またはアクセス制限した環境変数として設定してください。

```powershell
$env:APP_PUBLIC_BASE_URL='https://社内で利用する勤怠管理システムのURL'

$env:OUTLOOK_INTEGRATION_ENABLED='true'
$env:OUTLOOK_TENANT_ID='<Microsoft EntraテナントID>'
$env:OUTLOOK_CLIENT_ID='<アプリケーションID>'
$env:OUTLOOK_CLIENT_SECRET='<クライアントシークレット>'
$env:OUTLOOK_SENDER='attendance@example.co.jp'

$env:SLACK_INTEGRATION_ENABLED='true'
$env:SLACK_WEBHOOK_URL='<Slack Incoming Webhook URL>'
docker compose up -d --build
```

Microsoft Graphの `Mail.Send` アプリケーション権限は管理者同意が必要です。送信可能範囲は専用メールボックスへ制限してください。Slack Webhook URLは投稿権限を持つ秘密情報として扱ってください。管理者メニューには個別の連携ページを設けず、アプリ起動時にサーバー設定を読み込み、勤務表左メニューへSlackの利用可否を自動表示します。秘密情報そのものは画面やDBへ表示・保存しません。

## 過去Excel勤務表の取込

管理者権限を持つ利用者が管理者メニューの「Excel勤務表取込」から、`.xlsx` または `.xlsm`、必要な場合はExcelパスワードを指定します。提供済みの「勤務表」シートから対象年月、勤務時間、休憩、休暇、業務内容、システム番号、WG参加可否を読み取り、DBへ登録します。

- ファイル本体とパスワードは保存しません。
- ファイル名、SHA-256、取込先、対象年月、取込件数、操作者を監査目的で保存します。
- Excelの氏名・コードと取込先社員が異なる場合は警告します。
- 同じ月の勤務表がある場合は、確認なしに上書きしません。

## 50人規模のローカル検証データ

検証データは通常起動では作成されません。ローカル検証時だけ、次の環境変数を追加して起動します。

```powershell
$env:APP_DEMO_SEED_ENABLED='true'
$env:APP_DEMO_SEED_PASSWORD='<8～15文字の半角英数字の検証用パスワード>'
$env:APP_DEMO_SEED_COUNT='50'
$env:APP_DEMO_SEED_MONTHS='6'
docker compose up -d --build
```

`demo001`～`demo050` と、各社員の直近6か月分の勤務表が冪等に作成されます。本番環境では `APP_DEMO_SEED_ENABLED=false` のまま使用してください。

上野 豪のローカル確認用アカウント `test` と役職者確認用の`test02`をDBへ作る場合は、次の値も設定します。

```powershell
$env:APP_TEST_USER_ENABLED='true'
$env:APP_TEST_USER_PASSWORD='<8～15文字の半角英数字の一般ユーザー用パスワード>'
$env:APP_TEST_MANAGER_PASSWORD='<役職者用のローカル検証パスワード>'
```

この設定では一般権限の`test`（上野 豪）と、役職者権限の`test02`（役職者テスト）を冪等に作成します。短い検証パスワードはローカル確認専用とし、本番では`APP_TEST_USER_ENABLED=false`を維持してください。

## 停止

```powershell
docker compose down
```

データベースを含めて削除する `docker compose down -v` は通常運用では実行しないでください。

## Dockerなしデモ版（上司説明用）

会社PCへDocker、Java、Node.jsをインストールせずに見せるための、Windows用デモパッケージを作成できます。本番用のDocker・PostgreSQL構成は変更せず、デモ版だけ組み込みH2 DBと同梱Javaを使用します。

開発PCで次を1回実行します。

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\build-windows-demo.ps1
```

作成物は `dist\attendance-demo-windows.zip` です。会社PCではZIPを展開し、同梱の `はじめに・ログイン情報.txt` を確認して、PCだけで使う場合は `デモを起動.cmd`、同一Wi-Fiのスマートフォンから確認する場合は `スマホで確認.cmd` をダブルクリックします。

- 50人のデモ社員と直近2か月分の勤務表を組み込みDBへ登録済みです。
- 管理者と一般社員のパスワードはビルド時にランダム生成され、ZIP内の案内ファイルだけへ記載されます。
- 通常起動は `http://127.0.0.1:4280/` だけで待ち受けます。スマホ確認起動は `0.0.0.0` で待ち受け、黒い画面へ `http://PCのIPv4アドレス:4280/` を表示します。
- スマホ確認起動は信頼できる社内LAN・家庭内LANでの説明専用です。Windowsファイアウォールはプライベートネットワークだけを許可し、公衆Wi-Fiやインターネットへ公開しないでください。
- 入力内容は展開先の `data` フォルダーへ保存されます。
- 説明専用であり、本番データ・本番パスワードを入れないでください。説明後はZIPと展開先を削除してください。
- Outlook・Slackの実送信は、別途社内連携設定が必要です。

## 開発時の確認

Backend:

```powershell
cd backend
.\mvnw.cmd test
```

Frontend:

```powershell
cd frontend
npm install
npm run build
npm run lint
```

## セキュリティ上の前提

- 本番サーバーをインターネットへ直接公開しない。
- VPNとTLSを使用する。
- 本番では `SESSION_COOKIE_SECURE=true` を設定する。
- 初期管理者パスワードとDBパスワードは十分に長いランダム値にする。
- 削除用の共通固定パスワードは使用せず、操作中ユーザーのログインパスワードで再認証する。
- 本番ではOS、コンテナ、データベースの更新と日次バックアップを運用する。
- `.env`、バックアップ、ログをGitへ登録しない。
- 本番では検証データ投入を有効にしない。
- ID・パスワード入力を省略するパスキーまたは社内SSOは、本番のホスト名、TLS、社内ID基盤を確定してから導入する。端末指紋や共有端末の記憶だけでは本人識別を行わない。

## 設計資料

- `docs/01-current-workflow-analysis.md`: Excelの業務・マクロ分析
- `docs/02-requirements-draft.md`: 追加・変更要望を統合した現行要件定義
- `docs/03-excel-fidelity-acceptance.md`: Excelとの照合合格基準
- `docs/04-implementation-status.md`: 実装済み機能、Excel照合結果、残る制約
- `docs/05-system-design.md`: 画面、API、データ、セキュリティ、Excel・PDF・Outlook・Slack連携の設計
- `docs/deliverables/01_要件定義/要件定義書.md`: 社内利用へ向けた確定要件と本番移行条件
- `docs/deliverables/02_外部設計/外部設計書.md`: 画面、API、ファイル、外部連携の外部仕様
- `docs/deliverables/03_内部設計/内部設計書.md`: 実装構造、DB、計算、セキュリティ、コンテナの内部仕様
- `docs/deliverables/04_テスト/`: テスト仕様書兼実施結果とテスト実施報告書
- `docs/deliverables/05_運用/運用手順書.md`: 日次・月次運用、バックアップ、障害・セキュリティ対応
- `docs/deliverables/06_リリース/`: リリース手順書とリリースノート
- `docs/deliverables/07_セキュリティ/セキュリティ監査報告書.md`: アプリ・DB・コンテナの監査結果と残存リスク

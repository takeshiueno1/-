# 勤怠管理システム テスト実施報告書

## 1. 結論

2026-07-24時点の総合判定は「条件付き合格（保留あり）」である。実行・検査できたケースにNGはなく、検出した3件は修正後の再テストまで完了した。ただし、本番固有のVPN・TLS・MFA、実スマートフォン、Slack実投稿、利用者データを変更する受入シナリオ、Java依存関係の完全なSCAが未完了であるため、現時点で正式な本番リリース判定にはしない。

## 2. 受入テスト集計

| 項目 | 件数 |
|---|---:|
| テストケース | 99 |
| OK | 69 |
| NG | 0 |
| 保留 | 30 |
| OK率 | 69.7% |

Excel照合は16件中13件OK、3件保留。提供済み2026年6月勤務表の代表値は、平日180:00、休日0:00、半休1回、所定22日、所定176:00、過不足4:00で一致している。

## 3. 実行した自動検査

| 検査 | 結果 |
|---|---|
| Backend `mvnw test` | OK：29件、失敗0、エラー0 |
| Frontend `npm run build` | OK |
| Frontend `npm run lint` | OK |
| `npm audit --omit=dev` | OK：既知脆弱性0件 |
| `npm audit --audit-level=high` | OK：既知脆弱性0件 |
| `docker compose config --quiet` | OK |
| Dockerイメージ再ビルド | OK |
| コンテナヘルス | OK：DB/Backend healthy、Frontend running |
| HTTP `/` | OK：200 |
| HTTP `/actuator/health` | OK：200 |
| 未認証 `/api/admin/employees` | OK：401 |
| セキュリティヘッダー | OK：CSP、X-Frame-Options、X-Content-Type-Options、Referrer-Policy |
| Outlook偽トークンURL | OK：404、アクセスログへトークンなし |
| Java OWASP Dependency-Check | 保留：NVD初回取得が10分で完了せず |

## 4. ブラウザー実機確認

- ログイン画面、クエリロゴ、勤怠管理システム見出し
- 管理者ログインと右上ユーザーID
- ホーム、新規作成、過去参照、管理者メニュー
- 管理者メニューの4機能選択
- 50名の検証社員と上野 豪の登録状態
- 社員予測検索、候補表示、候補選択後の自動クローズ
- 管理者による他社員の代理表示
- 代理表示時にOutlook・Slack連絡欄を非表示
- 登録済み月の履歴とDBデータ保持
- Excel形式の勤務表、`1:00`休憩、休日表示、月次集計

## 5. 検出・修正した問題

| No. | 重要度 | 問題 | 対応 | 再テスト |
|---|---|---|---|---|
| FIX-001 | 中 | 日本語フォルダー名でComposeプロジェクト名を生成できない | `name: attendance`を追加 | OK |
| FIX-002 | 高 | Outlook一度限りトークンがアクセスログへ残る可能性 | 専用locationで`access_log off` | OK |
| FIX-003 | 中 | FrontendとDBが同一ネットワーク | `edge`と内部`data`へ分離 | OK |

## 6. 保留内容

- 30分無操作、5回ロック、期限切れ・期限警告を時間経過込みで確認する認証試験
- パスワード変更、利用者作成・無効化・再発行
- 実Excel・PDFを使用したDB取込異常系
- 実スマートフォンでのレイアウトと同一Wi-Fi接続
- 検証用Slack Webhookを使用した実投稿
- 印刷・PDF出力の縮尺と改ページ
- 新規作成から保存、過去編集、Outlookまでの利用部門E2E
- NVD APIキーを使用したJava依存関係のOWASP Dependency-Check
- 本番相当VPN、TLS、MFA、バックアップ復元、障害復旧

## 7. 正式リリース判定条件

1. 高優先度の保留ケースをすべてOKにする。
2. Java依存関係SCAを完了し、重大・高の既知脆弱性を0件にする。
3. 本番相当環境でVPN、TLS、MFA、バックアップ復元を確認する。
4. 利用部門がE2EとExcel照合の保留ケースを実施する。
5. 重大・高の未解決不具合が0件であることを責任者が承認する。
